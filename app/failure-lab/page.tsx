import { PageHeader } from "@/components/page-header";
import { FailureLabRunner } from "@/components/failure-lab-runner";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
export default function FailureLabPage() { return <main className="mx-auto flex max-w-5xl flex-col gap-6 px-4 py-8 sm:px-6 lg:px-8"><PageHeader eyebrow="Resilience" title="Failure lab" description="Run deterministic local fixtures for duplicate delivery, out-of-order delivery, invalid signatures, and model timeout. No external API is called." badge="Safe local injection" /><Card><CardHeader><CardTitle>Invariant checks</CardTitle><CardDescription>Captured payment truth must not regress, attribution stays unique, invalid signatures cannot mutate a case, and model failure falls back after two attempts.</CardDescription></CardHeader><CardContent><FailureLabRunner /></CardContent></Card></main>; }
