import { PageHeader } from "@/components/page-header";
import { FailureLabRunner } from "@/components/failure-lab-runner";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
export default function FailureLabPage() { return <main className="mx-auto flex max-w-5xl flex-col gap-6 px-4 py-8 sm:px-6 lg:px-8"><PageHeader eyebrow="Resilience" title="Failure lab" description="Run deterministic fixtures for duplicate delivery, out-of-order delivery, invalid signatures, and model timeout, then verify the deployed PostgreSQL safety backstops. No external provider API is called." badge="Safe local injection" /><Card><CardHeader><CardTitle>Invariant checks</CardTitle><CardDescription>Captured payment truth must not regress, attribution stays unique, invalid signatures cannot mutate a case, model failure must fall back after two attempts, and database constraints must remain installed.</CardDescription></CardHeader><CardContent><FailureLabRunner /></CardContent></Card></main>; }
