import { notFound } from "next/navigation";
import { prisma } from "@/lib/db/prisma";
import { SYNTHETIC_MERCHANT_ID } from "@/lib/config";
import { verifyAuditChain, type AuditEvent } from "@/lib/audit/chain";
import { PageHeader } from "@/components/page-header";
import { CaseControls } from "@/components/case-controls";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
export const dynamic = "force-dynamic";
export default async function CasePage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const item = await prisma.recoveryCase.findFirst({ where: { id, merchantId: SYNTHETIC_MERCHANT_ID }, include: { paymentAttempts: { orderBy: { attemptNumber: "asc" } }, plans: { orderBy: { createdAt: "desc" } }, actions: { orderBy: { createdAt: "desc" }, include: { inboxMessage: true } }, escalations: { orderBy: { createdAt: "desc" } }, auditEvents: { orderBy: { sequenceNumber: "asc" } }, attributions: true } }).catch(() => null);
  if (!item) notFound();
  const events: AuditEvent[] = item.auditEvents.map((event) => ({ event_id: event.id, case_id: event.caseId, sequence_number: event.sequenceNumber, timestamp: event.timestamp.toISOString(), actor_type: event.actorType, actor_id: event.actorId, event_type: event.eventType, input_refs: event.inputRefs as string[], decision: event.decision, reason_codes: event.reasonCodes, policy_snapshot: event.policySnapshot, model_metadata: event.modelMetadata ?? null, previous_hash: event.previousHash, event_hash: event.eventHash }));
  const audit = verifyAuditChain(events);
  const latestPlan = item.plans[0];
  return <main className="mx-auto flex max-w-7xl flex-col gap-6 px-4 py-8 sm:px-6 lg:px-8"><PageHeader eyebrow={`Case / ${item.mode}`} title={item.externalCaseId} description={`${item.orderId} · ${item.failureDescription}`} badge={`${item.paymentState} / ${item.recoveryState}`} /><section className="grid gap-4 md:grid-cols-3"><Info label="Amount at risk" value={`₹${(item.amountPaise / 100).toLocaleString("en-IN")}`} /><Info label="Consent / risk" value={`${item.consentStatus} / ${item.riskFlag}`} /><Info label="Audit chain" value={audit.valid ? `Verified · ${events.length} events` : `Invalid at ${audit.invalidSequence}`} /></section><Card><CardHeader><CardTitle>Operator controls</CardTitle><CardDescription>Every mutation is idempotent and appends audit evidence in the same database transaction. Proof actions require explicit approval.</CardDescription></CardHeader><CardContent><CaseControls caseId={item.id} batchId={item.batchId} mode={item.mode} state={item.recoveryState} /></CardContent></Card><section className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle>Latest frozen plan</CardTitle></CardHeader><CardContent>{latestPlan ? <div className="space-y-3 text-sm"><div className="flex items-center justify-between"><span className="text-muted-foreground">Planner</span><Badge variant={latestPlan.plannerType === "openai" ? "info" : "outline"}>{latestPlan.plannerType}</Badge></div><Row label="Diagnosis" value={latestPlan.diagnosis} /><Row label="Confidence" value={Number(latestPlan.confidence).toFixed(3)} /><Row label="Action" value={latestPlan.proposedAction} /><Row label="Delay" value={`${latestPlan.delayMinutes} minutes`} /><pre className="overflow-x-auto rounded-md bg-muted/50 p-3 text-xs">{JSON.stringify(latestPlan.policyDecision, null, 2)}</pre></div> : <p className="text-sm text-muted-foreground">No plan has been created.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Actions and simulated inbox</CardTitle></CardHeader><CardContent className="space-y-3">{item.actions.length ? item.actions.map((action) => <div key={action.id} className="rounded-md border p-3 text-sm"><div className="flex justify-between"><strong>{action.type}</strong><Badge variant="outline">{action.state}</Badge></div>{action.externalUrl ? <a className="mt-2 block break-all text-primary underline" href={action.externalUrl} target="_blank" rel="noreferrer">Open Razorpay test checkout</a> : null}{action.inboxMessage ? <p className="mt-2 text-muted-foreground">Inbox: {action.inboxMessage.body}</p> : null}</div>) : <p className="text-sm text-muted-foreground">No actions created.</p>}</CardContent></Card></section><Card><CardHeader><div className="flex items-center justify-between"><CardTitle>Immutable audit timeline</CardTitle><a className="text-sm text-primary underline" href={`/api/audit/${item.id}/export`}>Export JSON</a></div></CardHeader><CardContent className="space-y-3">{item.auditEvents.map((event) => <div key={event.id} className="grid gap-1 border-l-2 border-primary/30 pl-4 text-sm sm:grid-cols-[4rem_11rem_1fr]"><span className="font-mono text-xs text-muted-foreground">#{event.sequenceNumber}</span><span className="font-medium">{event.eventType}</span><span className="text-muted-foreground">{event.reasonCodes.join(", ")}</span></div>)}</CardContent></Card></main>;
}
function Info({ label, value }: { label: string; value: string }) { return <Card className="gap-2 py-5"><CardHeader><CardDescription>{label}</CardDescription></CardHeader><CardContent className="font-mono text-sm">{value}</CardContent></Card>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-4 border-b pb-2"><span className="text-muted-foreground">{label}</span><span className="font-mono">{value}</span></div>; }
